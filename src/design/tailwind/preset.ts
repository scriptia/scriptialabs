export const tailwindThemeBridge = {
  colors: {
    background: 'hsl(var(--color-background) / <alpha-value>)',
    surface: 'hsl(var(--color-surface) / <alpha-value>)',
    brand: 'hsl(var(--color-brand) / <alpha-value>)'
  }
} as const;
